meta {
  name: Create Service
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/services
  body: json
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  "projectId": 4,
  "name": "Incident API",
  "status": "HEALTHY"
}
